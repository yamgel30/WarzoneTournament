@page "/leaderboard"
@page "/leaderboard/{TournamentId:guid}"
@using WarzoneTournament.Application.Common.Interfaces
@using WarzoneTournament.Application.DTOs.Leaderboard
@using WarzoneTournament.Application.DTOs.Tournament
@using Microsoft.AspNetCore.SignalR.Client
@using System.Text.Json
@inject ILeaderboardService LeaderboardService
@inject ITournamentService TournamentService
@inject NavigationManager NavManager
@inject IJSRuntime JS
@implements IAsyncDisposable

<PageTitle>Live Leaderboard - Warzone</PageTitle>

@* ── Full-screen background image ── *@
@if (!string.IsNullOrEmpty(_tournamentDto?.BannerImageUrl))
{
    <div style="position:fixed;inset:0;z-index:-1;
                background-image:url('@_tournamentDto.BannerImageUrl');
                background-size:cover;background-position:center;
                opacity:0.13;pointer-events:none"></div>
}

@* ── Match Point / Winner overlay ── *@
@if (_showOverlay)
{
    <div class="@(_overlayIsWinner ? "mp-overlay mp-overlay--winner" : "mp-overlay mp-overlay--matchpoint")"
         @onclick="DismissOverlay">
        <div class="mp-overlay__inner">
            @if (_overlayIsWinner)
            {
                <div class="mp-overlay__icon">🏆</div>
                <div class="mp-overlay__label">¡CAMPEÓN!</div>
                <div class="mp-overlay__team">@_overlayTeamName</div>
                <div class="mp-overlay__subtitle">¡Ganó el torneo con Match Point!</div>
            }
            else
            {
                <div class="mp-overlay__icon">⚡</div>
                <div class="mp-overlay__label">MATCH POINT</div>
                <div class="mp-overlay__team">@_overlayTeamName</div>
                <div class="mp-overlay__subtitle">Solo necesita ganar 1 partida más para ser campeón</div>
            }
            <div class="mp-overlay__dismiss">Toca para cerrar</div>
        </div>
    </div>
}

<div class="leaderboard-page">

    @* ── Tournament logo / gif ── *@
    @if (!string.IsNullOrEmpty(_tournamentDto?.LogoUrl))
    {
        <div style="display:flex;justify-content:center;margin-bottom:12px">
            <img src="@_tournamentDto.LogoUrl" alt="Logo"
                 style="max-height:140px;max-width:360px;object-fit:contain;filter:drop-shadow(0 0 18px rgba(0,0,0,0.6))" />
        </div>
    }

    @* ── Header ── *@
    <div class="page-header">
        <h1 class="page-title">
            Live Leaderboard
            @if (_isConnected) { <span class="live-indicator">● LIVE</span> }
        </h1>
        <div class="leaderboard-controls">
            <RadzenDropDown @bind-Value="_selectedTournamentId"
                            Data="@_tournaments"
                            TextProperty="Name" ValueProperty="Id"
                            Placeholder="Selecciona torneo..."
                            Style="width:280px"
                            Change="@OnTournamentChanged" />
            <RadzenButton Icon="refresh" ButtonStyle="ButtonStyle.Light"
                          Click="@RefreshLeaderboard" title="Refresh" />
        </div>
    </div>

    @if (_selectedTournamentId == Guid.Empty)
    {
        <div class="empty-state">
            <div class="empty-state__icon">🏆</div>
            <h3>Selecciona un Torneo</h3>
            <p>Elige un torneo para ver el leaderboard en vivo.</p>
        </div>
    }
    else if (_loading)
    {
        <LoadingSpinner />
    }
    else
    {
        @* ── Winner banner ── *@
        @if (_tournamentDto?.WinnerTeamId.HasValue == true)
        {
            var winner = _leaderboard.FirstOrDefault(e => e.TeamId == _tournamentDto.WinnerTeamId);
            <div style="background:linear-gradient(135deg,#f59e0b,#d97706);color:#fff;border-radius:12px;padding:20px 28px;margin-bottom:16px;display:flex;align-items:center;gap:16px;box-shadow:0 4px 24px rgba(245,158,11,0.35)">
                <span style="font-size:2.5em">🏆</span>
                <div>
                    <div style="font-size:1.4em;font-weight:800">¡CAMPEÓN!</div>
                    <div style="font-size:1.15em;font-weight:600;margin-top:2px">@(winner?.TeamName ?? "—")</div>
                    <div style="font-size:0.85em;opacity:0.9;margin-top:2px">Ganó el torneo con Match Point</div>
                </div>
            </div>
        }

        @* ── Global stats bar ── *@
        @if (_leaderboard.Any())
        {
            var totalMaps   = _leaderboard.Max(e => e.MatchesPlayed);
            var avgTeamKpM  = _leaderboard.Average(e => e.AverageKillsPerMatch);
            var avgPlayerKpM = _playerLeaderboard.Any() ? _playerLeaderboard.Average(p => p.KillsPerMatch) : 0;
            var totalKills  = _leaderboard.Sum(e => e.TotalKills);
            <div class="lb-stats-bar">
                <div class="lb-stat"><span class="lb-stat__n">@totalMaps</span><span class="lb-stat__l">Mapas</span></div>
                <div class="lb-stat"><span class="lb-stat__n">@totalKills</span><span class="lb-stat__l">Kills totales</span></div>
                <div class="lb-stat"><span class="lb-stat__n">@avgTeamKpM.ToString("0.#")</span><span class="lb-stat__l">Avg kills/equipo/mapa</span></div>
                <div class="lb-stat"><span class="lb-stat__n">@avgPlayerKpM.ToString("0.#")</span><span class="lb-stat__l">Avg kills/jugador/mapa</span></div>
            </div>
        }

        @* ── Tabs ── *@
        <RadzenTabs @bind-SelectedIndex="_activeTab" style="margin-top:4px">
            <Tabs>

                @* ════ Tab 1 — Leaderboard ════ *@
                <RadzenTabsItem Text="🏅 Leaderboard">
                    <div class="lb-grid" style="margin-top:14px">

                        @* LEFT — Teams *@
                        <div class="lb-section">
                            <div class="lb-section-header">
                                <span>🏅 Equipos</span>
                                @if (_leaderboard.Any(e => e.IsMatchPoint))
                                {
                                    <span class="mp-active-badge">● MATCH POINT ACTIVO</span>
                                }
                            </div>

                            @if (!_leaderboard.Any())
                            {
                                <div class="empty-state" style="margin-top:16px">
                                    <div class="empty-state__icon">📊</div>
                                    <h3>Sin resultados aún</h3>
                                </div>
                            }
                            else
                            {
                                <div class="lb-table">
                                    <div class="lb-thead">
                                        <span class="lbc-rank">#</span>
                                        <span class="lbc-team">Equipo</span>
                                        <span class="lbc-num lbc-pts">Pts</span>
                                        <span class="lbc-num">Kills</span>
                                        <span class="lbc-num">K/Mapa</span>
                                        <span class="lbc-num">Mapas</span>
                                        <span class="lbc-num">Mejor</span>
                                    </div>
                                    @foreach (var entry in _leaderboard)
                                    {
                                        <div class="lb-row @GetRowClass(entry.Rank) @(entry.IsMatchPoint ? "lb-row--mp" : "")">
                                            <div class="lbc-rank">
                                                <div class="rank-badge rank-@(entry.Rank <= 3 ? entry.Rank.ToString() : "other")">
                                                    @if (entry.Rank == 1)     { <span>🥇</span> }
                                                    else if (entry.Rank == 2) { <span>🥈</span> }
                                                    else if (entry.Rank == 3) { <span>🥉</span> }
                                                    else                       { <span>@entry.Rank</span> }
                                                </div>
                                            </div>
                                            <div class="lbc-team">
                                                @if (!string.IsNullOrEmpty(entry.TeamLogoUrl))
                                                { <img src="@entry.TeamLogoUrl" alt="@entry.TeamName" class="team-logo-xs" /> }
                                                <div class="team-name-info">
                                                    @if (!string.IsNullOrEmpty(entry.TeamTag))
                                                    { <span class="team-tag">[<span>@entry.TeamTag</span>]</span> }
                                                    <span class="team-name">@entry.TeamName</span>
                                                    @if (entry.IsMatchPoint)
                                                    { <span class="mp-badge-inline">MP</span> }
                                                </div>
                                            </div>
                                            <div class="lbc-num lbc-pts points-total">@entry.TotalPoints.ToString("0.##")</div>
                                            <div class="lbc-num">@entry.TotalKills</div>
                                            <div class="lbc-num">@entry.AverageKillsPerMatch.ToString("0.#")</div>
                                            <div class="lbc-num">@entry.MatchesPlayed</div>
                                            <div class="lbc-num">
                                                @if (entry.BestPlacement > 0) { <span class="placement-value">#@entry.BestPlacement</span> }
                                                else { <span>—</span> }
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                        </div>

                        @* RIGHT — Players *@
                        <div class="lb-section">
                            <div class="lb-section-header">
                                <span>🎯 Top Fraggers</span>
                            </div>

                            @if (!_playerLeaderboard.Any())
                            {
                                <div class="empty-state" style="margin-top:16px">
                                    <div class="empty-state__icon">🎮</div>
                                    <h3>Sin estadísticas aún</h3>
                                    <p>Ingresa kills por jugador para ver este ranking.</p>
                                </div>
                            }
                            else
                            {
                                <div class="lb-table">
                                    <div class="lb-thead">
                                        <span class="lbc-rank">#</span>
                                        <span class="lbc-team">Jugador</span>
                                        <span class="lbc-num lbc-pts">Kills</span>
                                        <span class="lbc-num">K/Mapa</span>
                                        <span class="lbc-num">Mapas</span>
                                    </div>
                                    @foreach (var p in _playerLeaderboard)
                                    {
                                        <div class="lb-row @(p.Rank <= 3 ? GetRowClass(p.Rank) : "")">
                                            <div class="lbc-rank">
                                                <div class="rank-badge rank-@(p.Rank <= 3 ? p.Rank.ToString() : "other")">
                                                    @if (p.Rank == 1)     { <span>🥇</span> }
                                                    else if (p.Rank == 2) { <span>🥈</span> }
                                                    else if (p.Rank == 3) { <span>🥉</span> }
                                                    else                   { <span>@p.Rank</span> }
                                                </div>
                                            </div>
                                            <div class="lbc-team">
                                                <div class="team-name-info">
                                                    <span class="team-name">@p.Username</span>
                                                    @if (!string.IsNullOrEmpty(p.TeamTag))
                                                    { <span class="team-tag" style="margin-left:4px">[@p.TeamTag]</span> }
                                                </div>
                                                @if (!string.IsNullOrEmpty(p.TeamName))
                                                { <div style="font-size:0.78em;color:var(--rz-text-secondary-color)">@p.TeamName</div> }
                                            </div>
                                            <div class="lbc-num lbc-pts points-total">@p.TotalKills</div>
                                            <div class="lbc-num">@p.KillsPerMatch.ToString("0.#")</div>
                                            <div class="lbc-num">@p.MatchesPlayed</div>
                                        </div>
                                    }
                                </div>
                            }
                        </div>

                    </div>
                </RadzenTabsItem>

                @* ════ Tab 2 — Puntuación ════ *@
                <RadzenTabsItem Text="📋 Puntuación">
                    <div style="max-width:680px;margin:16px auto 0">

                        @* Formula explanation *@
                        <RadzenCard class="mb-3" style="border-left:4px solid var(--rz-primary)">
                            <h3 style="margin:0 0 8px">Fórmula de puntos</h3>
                            <div style="font-size:1.05em;background:var(--rz-base-background-color);padding:12px 16px;border-radius:6px;font-family:monospace">
                                Pts = Kills × Multiplicador(posición) + Bonus
                            </div>
                            @if (_tournamentDto?.MatchPointThreshold is not null)
                            {
                                <div style="margin-top:10px;display:flex;align-items:center;gap:8px">
                                    <span style="background:#ef4444;color:#fff;font-size:0.75em;font-weight:800;padding:2px 8px;border-radius:4px;letter-spacing:0.04em">MATCH POINT</span>
                                    <span style="font-size:0.95em">se activa al alcanzar <strong>@_tournamentDto.MatchPointThreshold pts</strong> acumulados</span>
                                </div>
                            }
                        </RadzenCard>

                        @* Placement multipliers table *@
                        @if (_placementMultipliers.Any())
                        {
                            <RadzenCard>
                                <h3 style="margin:0 0 12px">Multiplicadores por posición</h3>
                                <div class="lb-table">
                                    <div style="display:grid;grid-template-columns:80px 1fr 1fr;background:var(--rz-grid-header-background-color,#1e1e2e);padding:7px 14px;font-size:0.78em;color:var(--rz-text-secondary-color);font-weight:600;text-transform:uppercase;letter-spacing:0.04em;gap:8px">
                                        <span>Posición</span>
                                        <span style="text-align:right">Multiplicador</span>
                                        <span style="text-align:right">Ej. 10 kills</span>
                                    </div>
                                    @foreach (var kv in _placementMultipliers.OrderBy(x => x.Key))
                                    {
                                        var pts = Math.Round(10 * kv.Value, 2);
                                        <div style="display:grid;grid-template-columns:80px 1fr 1fr;padding:8px 14px;border-bottom:1px solid var(--rz-grid-header-border-color);gap:8px;align-items:center">
                                            <div style="display:flex;align-items:center;gap:6px;font-weight:600">
                                                @if (kv.Key == 1)      { <span>🥇</span> }
                                                else if (kv.Key == 2)  { <span>🥈</span> }
                                                else if (kv.Key == 3)  { <span>🥉</span> }
                                                <span>#@kv.Key</span>
                                            </div>
                                            <div style="text-align:right;font-weight:600;color:var(--rz-primary)">×@kv.Value.ToString("0.##")</div>
                                            <div style="text-align:right;color:var(--rz-text-secondary-color);font-size:0.9em">@pts.ToString("0.##") pts</div>
                                        </div>
                                    }
                                </div>
                            </RadzenCard>
                        }
                        else
                        {
                            <RadzenAlert AlertStyle="AlertStyle.Info" ShowIcon="true" Variant="Variant.Flat">
                                No hay multiplicadores configurados para este torneo.
                            </RadzenAlert>
                        }
                    </div>
                </RadzenTabsItem>

            </Tabs>
        </RadzenTabs>
    }
</div>

<style>
    /* ── Stats bar ── */
    .lb-stats-bar {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        margin-bottom: 4px;
    }
    .lb-stat {
        display: flex;
        flex-direction: column;
        align-items: center;
        background: var(--rz-base-background-color);
        border: 1px solid var(--rz-grid-header-border-color);
        border-radius: 8px;
        padding: 8px 18px;
        min-width: 110px;
        flex: 1;
    }
    .lb-stat__n {
        font-size: 1.5em;
        font-weight: 800;
        color: var(--rz-primary);
        line-height: 1;
    }
    .lb-stat__l {
        font-size: 0.72em;
        color: var(--rz-text-secondary-color);
        margin-top: 3px;
        text-align: center;
    }

    /* ── Two-column grid ── */
    .lb-grid {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
        gap: 20px;
        align-items: start;
    }
    @@media (max-width: 860px) {
        .lb-grid { grid-template-columns: 1fr; }
        .lb-stats-bar { gap: 8px; }
        .lb-stat { min-width: 80px; padding: 6px 10px; }
    }

    .lb-section-header {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 1.05em;
        font-weight: 700;
        margin-bottom: 10px;
    }

    /* ── Table ── */
    .lb-table { border-radius: 10px; overflow: hidden; }

    .lb-thead {
        display: grid;
        grid-template-columns: 48px 1fr 72px 60px 60px 52px 52px;
        background: var(--rz-grid-header-background-color, #1e1e2e);
        padding: 7px 12px;
        font-size: 0.78em;
        color: var(--rz-text-secondary-color);
        font-weight: 600;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        gap: 4px;
    }

    /* Player table has fewer columns */
    .lb-section:last-child .lb-thead {
        grid-template-columns: 48px 1fr 68px 60px 52px;
    }
    .lb-section:last-child .lb-row {
        grid-template-columns: 48px 1fr 68px 60px 52px;
    }

    .lb-row {
        display: grid;
        grid-template-columns: 48px 1fr 72px 60px 60px 52px 52px;
        align-items: center;
        padding: 8px 12px;
        border-bottom: 1px solid var(--rz-grid-header-border-color);
        gap: 4px;
        transition: background 0.15s;
    }
    .lb-row:last-child { border-bottom: none; }
    .lb-row:hover { background: rgba(255,255,255,0.03); }

    .lb-row--mp {
        border-left: 3px solid #ef4444 !important;
        background: rgba(239,68,68,0.07) !important;
    }

    .lbc-rank  { display: flex; align-items: center; justify-content: center; }
    .lbc-team  { display: flex; flex-direction: column; justify-content: center; overflow: hidden; }
    .lbc-num   { text-align: right; font-size: 0.92em; }
    .lbc-pts   { font-weight: 700; color: var(--rz-primary); }

    /* MP badges */
    .mp-active-badge {
        background: #ef4444;
        color: #fff;
        font-size: 0.68em;
        font-weight: 800;
        padding: 2px 8px;
        border-radius: 4px;
        letter-spacing: 0.04em;
        animation: mp-pulse 1.5s ease-in-out infinite;
    }
    .mp-badge-inline {
        background: #ef4444;
        color: #fff;
        font-size: 0.62em;
        font-weight: 800;
        padding: 2px 5px;
        border-radius: 3px;
        letter-spacing: 0.05em;
        margin-left: 5px;
        vertical-align: middle;
        animation: mp-pulse 1.5s ease-in-out infinite;
    }

    @@keyframes mp-pulse {
        0%, 100% { opacity: 1; }
        50%       { opacity: 0.5; }
    }

    /* ── Overlay ── */
    .mp-overlay {
        position: fixed; inset: 0; z-index: 9999;
        display: flex; align-items: center; justify-content: center;
        cursor: pointer;
        animation: overlay-in 0.35s cubic-bezier(0.34,1.56,0.64,1);
    }
    @@keyframes overlay-in {
        from { opacity: 0; transform: scale(0.85); }
        to   { opacity: 1; transform: scale(1); }
    }
    .mp-overlay--matchpoint { background: rgba(0,0,0,0.82); }
    .mp-overlay--winner     { background: rgba(0,0,0,0.88); }

    .mp-overlay__inner {
        text-align: center; padding: 40px 56px;
        border-radius: 20px; max-width: 560px; width: 90%;
        animation: card-bounce 0.4s cubic-bezier(0.34,1.56,0.64,1);
    }
    @@keyframes card-bounce {
        from { transform: translateY(40px) scale(0.9); }
        to   { transform: translateY(0) scale(1); }
    }
    .mp-overlay--matchpoint .mp-overlay__inner {
        background: linear-gradient(135deg,#1a0505,#3b0000);
        border: 2px solid #ef4444;
        box-shadow: 0 0 60px rgba(239,68,68,0.5), 0 0 120px rgba(239,68,68,0.2);
    }
    .mp-overlay--winner .mp-overlay__inner {
        background: linear-gradient(135deg,#1a1000,#3d2500);
        border: 2px solid #f59e0b;
        box-shadow: 0 0 60px rgba(245,158,11,0.5), 0 0 120px rgba(245,158,11,0.2);
    }

    .mp-overlay__icon {
        font-size: 4em; margin-bottom: 8px;
        animation: icon-pop 0.5s cubic-bezier(0.34,1.56,0.64,1) 0.15s both;
    }
    @@keyframes icon-pop {
        from { transform: scale(0); }
        to   { transform: scale(1); }
    }
    .mp-overlay__label {
        font-size: 1em; font-weight: 900; letter-spacing: 0.25em;
        text-transform: uppercase; margin-bottom: 12px;
    }
    .mp-overlay--matchpoint .mp-overlay__label { color: #ef4444; }
    .mp-overlay--winner     .mp-overlay__label { color: #f59e0b; }

    .mp-overlay__team {
        font-size: 2.4em; font-weight: 900; color: #fff;
        line-height: 1.1; margin-bottom: 12px;
        animation: glow-text 1.5s ease-in-out infinite alternate;
    }
    .mp-overlay--matchpoint .mp-overlay__team { text-shadow: 0 0 30px #ef4444; }
    .mp-overlay--winner     .mp-overlay__team { text-shadow: 0 0 30px #f59e0b; }
    @@keyframes glow-text {
        from { filter: brightness(1); }
        to   { filter: brightness(1.3); }
    }
    .mp-overlay__subtitle { font-size: 0.95em; color: rgba(255,255,255,0.7); margin-bottom: 24px; }
    .mp-overlay__dismiss  { font-size: 0.75em; color: rgba(255,255,255,0.3); letter-spacing: 0.05em; }
</style>

@code {
    [Parameter] public Guid TournamentId { get; set; }

    private bool _loading = false;
    private bool _isConnected = false;
    private int _activeTab = 0;
    private List<LeaderboardEntryDto> _leaderboard = new();
    private List<PlayerLeaderboardEntryDto> _playerLeaderboard = new();
    private List<TournamentListDto> _tournaments = new();
    private Guid _selectedTournamentId;
    private TournamentDto? _tournamentDto;
    private Dictionary<int, double> _placementMultipliers = new();
    private HubConnection? _hubConnection;

    // Overlay
    private bool _showOverlay = false;
    private bool _overlayIsWinner = false;
    private string _overlayTeamName = "";
    private HashSet<Guid> _prevMatchPointIds = new();
    private bool _prevWasCompleted = false;
    private bool _isFirstLoad = true;
    private Queue<(string name, bool isWinner)> _overlayQueue = new();
    private CancellationTokenSource? _overlayDismissCts;

    protected override async Task OnInitializedAsync()
    {
        await LoadTournamentsAsync();
        if (TournamentId != Guid.Empty)
        {
            _selectedTournamentId = TournamentId;
            await LoadLeaderboardAsync();
        }
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
            await ConnectToSignalRAsync();
    }

    private async Task LoadTournamentsAsync()
    {
        var result = await TournamentService.GetTournamentsAsync(new TournamentQueryDto { PageSize = 50 });
        if (result.IsSuccess) _tournaments = result.Value.Items.ToList();
    }

    private async Task LoadLeaderboardAsync()
    {
        if (_selectedTournamentId == Guid.Empty) return;
        _loading = true;
        try
        {
            var teamResult       = await LeaderboardService.GetTournamentLeaderboardAsync(_selectedTournamentId);
            var playerResult     = await LeaderboardService.GetPlayerLeaderboardAsync(_selectedTournamentId);
            var tournamentResult = await TournamentService.GetTournamentByIdAsync(_selectedTournamentId);

            bool hasWinner = tournamentResult.IsSuccess && tournamentResult.Value.WinnerTeamId.HasValue;

            if (teamResult.IsSuccess)
            {
                var newLb = teamResult.Value.ToList();
                var newMp = newLb.Where(e => e.IsMatchPoint).Select(e => e.TeamId).ToHashSet();

                if (!_isFirstLoad && !hasWinner)
                {
                    foreach (var id in newMp.Except(_prevMatchPointIds))
                    {
                        var name = newLb.FirstOrDefault(e => e.TeamId == id)?.TeamName ?? "Equipo";
                        _overlayQueue.Enqueue((name, false));
                    }
                }
                _prevMatchPointIds = newMp;
                _leaderboard = newLb;
            }

            if (playerResult.IsSuccess) _playerLeaderboard = playerResult.Value.ToList();

            if (tournamentResult.IsSuccess)
            {
                _tournamentDto = tournamentResult.Value;

                // Parse placement multipliers for Scoring tab
                try { _placementMultipliers = JsonSerializer.Deserialize<Dictionary<int, double>>(_tournamentDto.PlacementPointsJson) ?? new(); }
                catch { _placementMultipliers = new(); }

                if (!_prevWasCompleted && _tournamentDto.WinnerTeamId.HasValue)
                {
                    var name = _leaderboard.FirstOrDefault(e => e.TeamId == _tournamentDto.WinnerTeamId)?.TeamName ?? "Equipo";
                    _overlayQueue.Enqueue((name, true));
                    _prevWasCompleted = true;
                }
            }

            _isFirstLoad = false;
            await ProcessOverlayQueueAsync();
        }
        finally { _loading = false; }
    }

    private async Task ProcessOverlayQueueAsync()
    {
        if (_showOverlay || !_overlayQueue.Any()) return;
        var (name, isWinner) = _overlayQueue.Dequeue();
        _overlayTeamName = name;
        _overlayIsWinner = isWinner;
        _showOverlay = true;
        await InvokeAsync(StateHasChanged);
        try
        {
            if (isWinner)
            {
                await JS.InvokeVoidAsync("launchWinnerConfetti");
                await JS.InvokeVoidAsync("playWinnerSound");
            }
            else
            {
                await JS.InvokeVoidAsync("launchMatchPointConfetti");
                await JS.InvokeVoidAsync("playMatchPointSound");
            }
        }
        catch { }
        _overlayDismissCts?.Cancel();
        _overlayDismissCts = new CancellationTokenSource();
        var token = _overlayDismissCts.Token;
        _ = Task.Delay(6000, token).ContinueWith(async _ =>
        {
            if (!token.IsCancellationRequested)
                await InvokeAsync(async () => { await DismissOverlay(); });
        }, TaskScheduler.Default);
    }

    private async Task DismissOverlay()
    {
        _overlayDismissCts?.Cancel();
        _showOverlay = false;
        StateHasChanged();
        await Task.Delay(200);
        if (_overlayQueue.Any()) await ProcessOverlayQueueAsync();
    }

    private async Task ConnectToSignalRAsync()
    {
        _hubConnection = new HubConnectionBuilder()
            .WithUrl(NavManager.ToAbsoluteUri("/hubs/tournament"))
            .WithAutomaticReconnect()
            .Build();

        _hubConnection.On<Guid, object>("LeaderboardUpdated", async (tournamentId, _) =>
        {
            if (tournamentId == _selectedTournamentId)
            {
                await LoadLeaderboardAsync();
                await InvokeAsync(StateHasChanged);
            }
        });

        _hubConnection.On<Guid, string>("TournamentStatusChanged", async (tournamentId, status) =>
        {
            if (tournamentId == _selectedTournamentId && status == "Completed")
            {
                await LoadLeaderboardAsync();
                await InvokeAsync(StateHasChanged);
            }
        });

        _hubConnection.Reconnecting += _ => { _isConnected = false; InvokeAsync(StateHasChanged); return Task.CompletedTask; };
        _hubConnection.Reconnected  += async _ =>
        {
            _isConnected = true;
            if (_selectedTournamentId != Guid.Empty)
                await _hubConnection.SendAsync("JoinTournamentGroup", _selectedTournamentId.ToString());
            await InvokeAsync(StateHasChanged);
        };

        try
        {
            await _hubConnection.StartAsync();
            _isConnected = true;
            if (_selectedTournamentId != Guid.Empty)
                await _hubConnection.SendAsync("JoinTournamentGroup", _selectedTournamentId.ToString());
        }
        catch { _isConnected = false; }
    }

    private async Task OnTournamentChanged(object? _)
    {
        _prevMatchPointIds.Clear();
        _prevWasCompleted = false;
        _isFirstLoad = true;
        _overlayQueue.Clear();
        _showOverlay = false;
        _placementMultipliers.Clear();
        if (_hubConnection?.State == HubConnectionState.Connected && _selectedTournamentId != Guid.Empty)
            await _hubConnection.SendAsync("JoinTournamentGroup", _selectedTournamentId.ToString());
        await LoadLeaderboardAsync();
    }

    private async Task RefreshLeaderboard() => await LoadLeaderboardAsync();

    private string GetRowClass(int rank) => rank switch
    {
        1 => "rank-gold",
        2 => "rank-silver",
        3 => "rank-bronze",
        _ => ""
    };

    public async ValueTask DisposeAsync()
    {
        _overlayDismissCts?.Cancel();
        if (_hubConnection is not null) await _hubConnection.DisposeAsync();
    }
}
